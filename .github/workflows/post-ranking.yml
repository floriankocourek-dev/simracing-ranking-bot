name: Post Ranking to Discord

# Auslöser:
#  - workflow_dispatch: manueller Knopf im Actions-Tab
#  - repository_dispatch (type "recalc"): aus dem Google Sheet per API
#    (Klick auf "Ratings & Ranking neu berechnen")
on:
  workflow_dispatch:
  repository_dispatch:
    types: [recalc]

permissions:
  contents: write   # damit snapshot.json zurueckgeschrieben werden darf

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Repo auschecken
        uses: actions/checkout@v4

      - name: Node einrichten
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Ranking nach Discord posten
        env:
          SHEET_CSV_URL: ${{ secrets.SHEET_CSV_URL }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: node post-ranking.mjs

      - name: Webapp-Daten neu erzeugen
        run: node build-data.mjs

      - name: Aktualisierte Daten zurueckschreiben
        run: |
          git add snapshot.json docs/data
          if [ -n "$(git status --porcelain snapshot.json docs/data)" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git commit -m "Update ranking snapshot + webapp data"
            git push
          else
            echo "Kein Update noetig."
          fi
